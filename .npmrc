# Electron binary downloads through the npmmirror mirror instead of GitHub,
# which is reachable in environments where github.com release downloads are
# slow or blocked. pnpm applies this file for the desktop package.
electron_mirror=https://npmmirror.com/mirrors/electron/
